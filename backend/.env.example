PORT=4000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=online_medical_store_finder

# Frontend URL for CORS
CORS_ORIGIN=http://localhost:5173

# MVP-only: naive admin token for write endpoint
ADMIN_TOKEN=change-me

